"""#variable declration

calculation_of_days = 24
name_of_unit = "hours"

# declaration and calculation  of function units
def days_to_units(custom_message,number_of_days):

    # if condition statement
   
    if number_of_days > 0:

        return f"{custom_message} {number_of_days} days are {number_of_days * calculation_of_days } {name_of_unit}"
    
    else:
        return "You entered a negative value!"


# user input 

user_input = input("Enter number of days: \n")
user_input_number = int(user_input)

# function calling


calculated_value = days_to_units ("Motivation: ", user_input_number)
print(calculated_value)






#QUESTION ONE

""""""calculation_of_days = 24 
calculation_of_units = "hours"

#function declartion

def convert_days(custom_message, number_of_days):
  
  if number_of_days > 0:

     return f"{custom_message} {number_of_days} days are {number_of_days * calculation_of_days} {calculation_of_units}"

  else:
     return "Invalid number of days"

#user input 

user_input = input("Enter number of days: \n")
user_input_number = int(user_input)

#Call function

converted_input = convert_days ("Focus: ",  user_input_number)
print(converted_input)"""

"""

calculation_of_days = 24 
calculation_of_units = "hours"

def convert_days(custom_message, number_of_days):
    if number_of_days > 0:
        return f"{custom_message} {number_of_days} days are {number_of_days * calculation_of_days} {calculation_of_units}"
    else:
        return "Invalid number of days"

user_input = input("Enter number of days: \n").strip()

if user_input.isdigit():
    converted_input = convert_days("Focus:", int(user_input))
    print(converted_input)
else:
    print("Invalid input")


# QUESTION

"""



# QUESTION 2 

calculation_of_days = 24
calculation_of_units = "hour"

#function declaration

def multi_days_converter (custom_message, number_of_days):
  if number_of_days > 0:
     return f"{custom_message} {number_of_days} days are {number_of_days * calculation_of_days} {calculation_of_units}"
  
  elif number_of_days == 0:
     return "you enetered a 0, please enter a valid number"
     
  else:
     return "Invalid number of days"
  
  #user input

user_input = input("enter days repeated by comma:  \n").strip()
user_input_number = int(user_input)

calculated_input = multi_days_converter ("Focus: ", user_input_number)

print(calculated_input)


# Organized Code


calculation_of_days = 24
calculation_of_units = "hour"

# function declaration
def multi_days_converter(custom_message, number_of_days):
    return f"{custom_message} {number_of_days} days are {number_of_days * calculation_of_days} {calculation_of_units}"


# validate and execute
def validate_and_execute():
    if user_input.isdigit():
        user_input_number = int(user_input)

        if user_input_number > 0:
            calculated_input = multi_days_converter("Focus:", user_input_number)
            print(calculated_input)

        elif user_input_number == 0:
            print("You entered 0, please enter a valid number")

        else:
            print("Invalid number of days")

    else:
        print("Hey User, your input is not valid!")


# user input
user_input = input("Enter number of days:\n")

# calling function
validate_and_execute()


# Error Control


calculation_of_days = 24
calculation_of_units = "hour"

# function declaration
def multi_days_converter(custom_message, number_of_days):
    return f"{custom_message} {number_of_days} days are {number_of_days * calculation_of_days} {calculation_of_units}"


# validate and execute
def validate_and_execute():
    try: 


        user_input_number = int(user_input)

        if user_input_number > 0:
            calculated_input = multi_days_converter("Focus:", user_input_number)
            print(calculated_input)

        elif user_input_number == 0:
            print("You entered 0, please enter a valid number")

        else:
            print("Invalid number of days")

    except ValueError: 
        print("Hey User, your input is not valid!")


# user input
user_input = input("Enter number of days:\n")

# calling function
validate_and_execute()










  










